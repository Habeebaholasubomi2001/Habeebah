"use strict";
// Return a Boolean if a number is divisible by 10

// const numDivisibleByTen = function (number) {
//   if (number / 10) {
//     console.log(`Divisible`);

//     // Boolean([return, value]);
//   } else {
//     console.log(`Not`);
//   }
// };

// // const numbers = prompt(`Check if this number is divisible by 10`);

// console.log(numDivisibleByTen(numbers));

// To get the last n elements in an array.
// array.slice(-n)

function myFunction() {
  const arr = [1, 2, 3, 4];
  return arr.slice(-2);
}
console.log(myFunction());
